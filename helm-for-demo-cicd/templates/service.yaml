apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-for-demo-cicd.fullname" . }}
  labels:
    {{- include "helm-for-demo-cicd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-for-demo-cicd.selectorLabels" . | nindent 4 }}
