apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-for-demo-cicd.fullname" . }}-test-connection"
  labels:
    {{- include "helm-for-demo-cicd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-for-demo-cicd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
